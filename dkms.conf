PACKAGE_NAME="la_ow_syscall"
PACKAGE_VERSION="0.0.1"

#if [ -f $kernel_source_dir/.config ]; then
#    . $kernel_source_dir/.config
#    if [ "${CONFIG_KALLSYMS:-n}" = "n" ]; then
#      BUILD_EXCLUSIVE_KERNEL="REQUIRES CONFIG_KALLSYMS"
#    fi
#    if [ "${CONFIG_KPROBES:-n}" = "n" ]; then
#      BUILD_EXCLUSIVE_KERNEL="REQUIRES CONFIG_KPROBES"
#    fi
#fi

BUILD_EXCLUSIVE_KERNEL_MIN="5.19"
BUILD_EXCLUSIVE_CONFIG="CONFIG_KALLSYMS CONFIG_KPROBES"

# Items below here should not have to change with each driver version
MAKE[0]="make KERNEL_DIR=${kernel_source_dir} all"
CLEAN="make clean"

BUILT_MODULE_NAME[0]="$PACKAGE_NAME"
DEST_MODULE_LOCATION[0]="/extra"

AUTOINSTALL="yes"
